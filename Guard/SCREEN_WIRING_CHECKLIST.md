# Guard App Screen Wiring Checklist (Production Audit)

Date: 2026-02-22

## Scope
- This file tracks Guard app production-readiness wiring status screen-by-screen.
- Approach mirrors the user app audit process: DB-first wiring, scoped RLS-safe reads/writes, no placeholder runtime behavior.
- Sequence (to be audited in order):
  - Auth + Home + Dashboard
  - Residents + Community Directory
  - Visitors + Entry/Exit Operations
  - Emergency + Incident Handling
  - Messaging + Calls
  - Profile + Settings

## Status Legend
- `Wired`: connected to DB with expected production behavior.
- `Partial`: connected, but missing contract fields, scope checks, or UX consistency.
- `Stub`: UI-only or sample-data behavior.
- `Broken`: wired path exists but blocks runtime behavior.

## Bootstrap Check
| Area | Evidence | Status | Notes |
|---|---|---|---|
| Expo SDK resolution | Guard startup error fixed by installing dependencies in `/Users/andromeda/casanirvana/Guard` | `Wired` | `expo` now resolves from installed modules |
| Runtime config load | `npx expo config --type public --json` succeeds in `/Users/andromeda/casanirvana/Guard` | `Wired` | Guard app can proceed to module audit/remediation |

## 1) Auth + Home + Dashboard
| Screen/Component | Data-Bound UI Elements | Current Wiring | Status | Wiring Checklist |
|---|---|---|---|---|
| `/Users/andromeda/casanirvana/Guard/contexts/GuardAuthContext.js` | Session bootstrap, guard identity hydration, auth gating | Rebuilt provider and enforced guard-only access (`users.role in ['guard','GUARD']` + `guards` profile + active status + community assignment). Invalid sessions are signed out deterministically. | `Wired` | Keep `isAuthenticated` dependent on both auth session and valid guard profile scope |
| `/Users/andromeda/casanirvana/Guard/screens/splashScreen.js` | Startup route decision to Auth vs Home | Removed render-time timeout; now uses auth context readiness and resets stack to `loginScreen`/`bottomTab` after bootstrap. | `Wired` | Do not push navigation from render; always route from deterministic auth state |
| `/Users/andromeda/casanirvana/Guard/screens/auth/emailLoginScreen.js` | Email/password sign-in + transition to app shell | Uses context `signIn`; progress callback is now completed on all paths; successful login resets nav stack to `bottomTab`. | `Wired` | Preserve always-complete button progress behavior to avoid stuck submit state |
| `/Users/andromeda/casanirvana/Guard/screens/auth/registerScreen.js` + `/Users/andromeda/casanirvana/Guard/hooks/useRegisterGuard.js` | Guard registration + post-register return-to-login flow | Registration stays DB-first (Auth + `users` trigger + `guards` insert), button progress resolves correctly, and successful signup now hands off to email login with an explicit awaiting-assignment notice. | `Partial` | Profile creation is wired, but community assignment remains an admin-side dependency for production onboarding |
| `/Users/andromeda/casanirvana/Guard/screens/auth/verificationScreen.js` | OTP verify/resend flow for phone sign-in | Replaced placeholder 4-digit bypass with real `supabase.auth.verifyOtp` and resend (`signInWithOtp`), plus timer and error handling. | `Wired` | Keep verification screen tied to real auth API only (no mock navigation bypass) |
| `/Users/andromeda/casanirvana/Guard/screens/homeScreen.js` | Visitor entry code + confirm action | Gate-pass code resolves against `visitor_passes.entry_code` scoped by authenticated guard `community_id` and forwards resolved pass to confirmation screen. | `Wired` | Keep alphanumeric code support, block invalid/non-actionable statuses, pass payload to confirm flow |
| `/Users/andromeda/casanirvana/Guard/screens/homeScreen.js` | Header identity + tenant scope + unread indicator | Replaced hardcoded guard/community header and mock unread dot with authenticated guard/community data and live `notifications` unread count subscription. | `Wired` | Keep home header fully tenant-scoped and avoid static profile/community placeholders |
| `/Users/andromeda/casanirvana/Guard/screens/notificationScreen.js` + `/Users/andromeda/casanirvana/Guard/hooks/useNotifications.js` | Notification inbox list, realtime updates, mark-read actions, mark-all-read action | Replaced mock notification list with DB-backed `notifications` query + realtime subscription scoped to authenticated guard; added swipe mark-read action and explicit mark-all-read control, with consistent unread-state handling (`read_at`/`is_read`/`read`). | `Wired` | Keep notification reads/writes scoped to `user_id = auth.uid()` and avoid local-only list mutations |
| `/Users/andromeda/casanirvana/Guard/screens/notificationDetailScreen.js` | Notification detail metadata + read/unread state mutation + share action | Replaced placeholder/local-only actions with real detail fetch fallback by id and policy-safe read/unread update writes to `notifications`; share action now uses persisted title/body content. | `Wired` | Keep detail updates limited to actor-owned notifications and preserve unread parity with home badge |
| `/Users/andromeda/casanirvana/Guard/screens/inOutScreen.js` | Checked-in/checked-out dashboard tabs + counts | Replaced count-by-full-list fetches with scoped aggregate count hook (`useVisitorPassCounts`) and kept tabs focused on status-specific list rendering. | `Wired` | Keep count queries lightweight and realtime-updated; avoid duplicated full dataset fetches for badge counts |
| `/Users/andromeda/casanirvana/Guard/components/checkedInTab.js` + `/Users/andromeda/casanirvana/Guard/components/checkedOutTab.js` | In/out list cards, empty/error/loading states, detail navigation payload | Unified DB-to-UI mapping with safe duration/time formatting and consistent detail payloads; fixed checked-out error rendering and removed static phone fallbacks. | `Wired` | Keep status lists strictly DB-driven (`checked_in`/`checked_out`) and avoid NaN duration rendering |
| `/Users/andromeda/casanirvana/Guard/screens/visitorDetailScreen.js` | In/out detail lifecycle rows (`scheduled`, `actual entry`, `actual exit`) + mark in/out actions | Exit timestamp is now displayed only after actual check-out (`actual_exit_time`/`checked_out_at`); removed schedule-to-exit conflation and aligned action visibility with real pass state. | `Wired` | Do not surface planned/scheduled end as actual exit; actual exit must come from guard check-out event |
| `/Users/andromeda/casanirvana/Guard/screens/qrScanner.js` | QR scan camera + decode path | Replaced mock scanner with `expo-camera` QR scan, DB lookup (`visitor_passes` by QR/id/entry_code), and guarded navigation to confirm screen. | `Wired` | Camera permission gate, duplicate-scan guard, actionable-status guard, scan retry action |
| `/Users/andromeda/casanirvana/Guard/screens/confirmScreen.js` | Visitor pass preview (QR, code, guest/unit/host/time) + allow action | Confirmation now resolves real pass details (`visitorPassId`, scanned QR, or entered code) and routes approved passes into `allowedScreen` with real DB IDs. | `Wired` | Remove hardcoded pass defaults, preserve legacy guest call-host path, block unresolved pass state |
| `/Users/andromeda/casanirvana/Guard/screens/allowedScreen.js` | Visitor check-in status mutation + resident notification | Uses canonical pass update and valid `notifications` schema (`title/body/notification_type/reference_id`) with nested home navigation fix, and now resolves host attribution from unit scope before persisting guard notes / success metadata. | `Wired` | Keep host attribution unit-backed for walk-in and pass-entry approval flows |
| `/Users/andromeda/casanirvana/Guard/screens/cancelledScreen.js` | Visitor denial status mutation + resident notification | Canonical deny status (`denied`) + valid notification payload + nested home navigation fix, with host attribution now resolved from unit scope before denial is persisted. | `Wired` | Keep denial notes and UI host labels sourced from unit-backed resident resolution |
| `/Users/andromeda/casanirvana/Guard/screens/guestEntryScreen.js` | Walk-in guest form fields (name/phone/time) + continue action | Form now validates required fields, normalizes phone input, and forwards ISO entry time into unit selection flow. | `Wired` | Keep strict field validation and preserve selected entry time across flow |
| `/Users/andromeda/casanirvana/Guard/screens/cabEntryScreen.js` | Cab driver/company/type/time + continue action | Cab entry is now a clean first-step form only: validates driver + 4-digit vehicle token, then hands off to unit selection / confirmation without legacy selected-flat confirmation branches on the same screen. | `Wired` | Keep cab confirmation centralized in `flatNoScreen` -> `entryConfirmationScreen` only |
| `/Users/andromeda/casanirvana/Guard/screens/deliveryEntryScreen.js` | Delivery person/company/time + continue action | Flow validates required fields and phone format, preserves selected time to confirmation/unit-selection, and forwards `unitId` when selected. | `Wired` | Replace remaining legacy unused state/hooks in this screen cleanup pass |
| `/Users/andromeda/casanirvana/Guard/screens/serviceEntryScreen.js` | Service provider/type/time + continue action | Flow validates required fields + phone, removes encoded-title workaround from primary path, and forwards ISO time to unit-selection. | `Wired` | Localize new validation strings in `Guard/languages` pass |
| `/Users/andromeda/casanirvana/Guard/screens/flatNoScreen.js` + `/Users/andromeda/casanirvana/Guard/components/flatNoTab.js` | Unit list, host display, block tabs, continue action | Unit list is DB-bound and host labels use batched resident lookup; continue action now preserves normalized ISO entry time + unit ID into confirmation. | `Wired` | Add server-side pagination/filtering only if unit counts become large |
| `/Users/andromeda/casanirvana/Guard/screens/entryConfirmationScreen.js` + `/Users/andromeda/casanirvana/Guard/screens/ringingScreen.js` | Final preview + resident-call decision (allow/deny) | Entry metadata is passed through consistently (`unitId`, `selectedTime`, `entryType`), and allow/deny paths create/finalize visitor pass IDs deterministically. | `Wired` | Keep timeout/retry UX in call simulation review pass |

## 2) Residents + Community Directory
| Screen/Component | Data-Bound UI Elements | Current Wiring | Status | Wiring Checklist |
|---|---|---|---|---|
| `/Users/andromeda/casanirvana/Guard/services/moduleSettingsService.js` + `/Users/andromeda/casanirvana/Guard/hooks/useGuardModuleAccess.js` | Guard module-toggle resolution (`resident_directory`, entry modules, emergency module) | Added scoped guard-aware module settings service (auth + community scoped cache, fail-closed known slugs) and shared access hook for resident-directory gating. | `Wired` | Keep guard module reads community-scoped and never default known slugs to enabled on missing data |
| `/Users/andromeda/casanirvana/Guard/hooks/useCommunityDirectoryMembers.js` | Community member directory query + realtime subscription | Directory query/subscription now accepts an `enabled` gate so resident lookups and invalidations stop cleanly when the resident-directory module is disabled. | `Wired` | Preserve disabled-query behavior to avoid hidden background reads for disabled modules |
| `/Users/andromeda/casanirvana/Guard/screens/chatScreen.js` | Residents tab visibility + resident search entrypoint | Residents top-tab and search icon now honor the `resident_directory` module toggle; chat conversations remain available even when the directory is disabled. | `Wired` | Keep chats independent from directory visibility so guards retain message access without exposing resident discovery |
| `/Users/andromeda/casanirvana/Guard/screens/searchScreen.js` | Resident search UI, quick message/call launch | Search route now blocks cleanly with a disabled-module state when resident directory is turned off for the guard's community; removed fake voice-search placeholder and replaced fake “recent search” list with real guard-scoped recent resident lookups. | `Wired` | Do not allow direct route access to bypass resident-directory module settings, and keep recent lookups device-scoped + guard-scoped |
| `/Users/andromeda/casanirvana/Guard/components/residentsTab.js` | Resident directory list, grouped blocks, quick message/call launch | Residents tab now respects module settings, shows a branded unavailable state when disabled, and normalizes resident avatar sources before routing into chat/call flows. | `Wired` | Keep resident discovery fail-closed whenever `resident_directory` is disabled and preserve consistent route image payloads |

## 3) Visitors + Entry/Exit Operations
| Screen/Component | Data-Bound UI Elements | Current Wiring | Status | Wiring Checklist |
|---|---|---|---|---|
| `/Users/andromeda/casanirvana/Guard/hooks/useVisitorPasses.js` | Pass list + status updates | `updatePassStatus` now throws on auth/RLS errors so UI can handle failures deterministically. | `Wired` | Keep guard-scoped realtime channel and status mutation behavior |
| `/Users/andromeda/casanirvana/Guard/hooks/useVisitorPasses.js` | Host-resident attribution for pass cards/details | Replaced per-pass N+1 `users` lookups with single batched `profiles` query per unit set (guard-scoped via RLS), then role-prioritized host selection. | `Wired` | Keep host lookup batched and community-scoped for performance and tenant correctness |
| `/Users/andromeda/casanirvana/Guard/hooks/useVisitorPassCounts.js` | In/out tab count badges | New lightweight count hook (`head` count queries + realtime invalidation) for `checked_in` and `checked_out`. | `Wired` | Maintain this as the only source for tab badge counts |
| `/Users/andromeda/casanirvana/Guard/services/inOutPassMapper.js` | Shared pass-to-card/detail mapper | New shared mapper standardizes block/unit labels, status-safe duration formatting, and payload shape between checked-in and checked-out tabs. | `Wired` | Keep list/detail mapping centralized to prevent divergence across tabs |
| `/Users/andromeda/casanirvana/Guard/services/visitorEntryService.js` | Pass lookup/resolution service for code + QR paths | New canonical service for pass resolution, status eligibility checks, and route-param mapping. | `Wired` | Single source of truth for visitor-entry lookup behavior |

## 4) Emergency + Incident Handling
| Screen/Component | Data-Bound UI Elements | Current Wiring | Status | Wiring Checklist |
|---|---|---|---|---|
| `/Users/andromeda/casanirvana/Guard/hooks/useEmergencyAlerts.js` | Emergency alerts list query + realtime subscription | New community-scoped query for `emergency_alerts` (joins `units`, `communities`, reporter profile) with realtime invalidation by community channel. | `Wired` | Keep guard scope strict to `guard.community_id`; avoid unscoped emergency reads |
| `/Users/andromeda/casanirvana/Guard/screens/emergencyScreen.js` | Alert cards, status filters, summary counters, detail navigation payload | Replaced local mock alerts with DB-backed list and polished In/Out-style card layout (status/priority/type/unit/reporter/time). | `Wired` | Keep list contract DB-first; if guard status mutation is needed, add dedicated policy-safe action flow rather than UI-only toggles |
| `/Users/andromeda/casanirvana/Guard/screens/emergencyDetailScreen.js` | Incident detail view and response actions | Wired guard triage actions (`acknowledge` -> `active`, `start investigation` -> `investigating`, `mark resolved` -> `resolved`) and `contact admin` escalation dispatch (community admin fan-out via `notifications` + `emergency_alert_recipients`) with RLS-safe community scoping and local timeline/status refresh. | `Wired` | Keep status transitions constrained to approved enum states and preserve `resolved_at/resolved_by` + recipient dispatch audit fields |

## 5) Messaging + Calls
| Screen/Component | Data-Bound UI Elements | Current Wiring | Status | Wiring Checklist |
|---|---|---|---|---|
| `/Users/andromeda/casanirvana/Guard/hooks/useMessages.js` | Message list, send, read receipts, conversation summaries, realtime sync | Aligned to canonical `messages` contract (`message_type`, `attachments`, `read/is_read`, `message_status`), object-form query invalidation, and profile-based conversation summaries from `profiles`; realtime now invalidates both active thread and conversation list. | `Wired` | Keep canonical payload fields on insert and preserve conversation invalidation coupling |
| `/Users/andromeda/casanirvana/Guard/components/chatsTab.js` | Chats tab list items (avatar/name/last message/time/unread) | Replaced static/demo chat list and random enhancement hook with DB-backed `useConversations` rendering. | `Wired` | Keep Chats tab strictly conversation-driven (directory remains in Residents tab) |
| `/Users/andromeda/casanirvana/Guard/screens/searchScreen.js` | Resident search results + last-message preview + quick call/chat actions | Search now uses real community directory members and conversation-derived last-message/unread previews (no synthetic chat enhancer data). | `Wired` | Keep preview source as `useConversations`; avoid reintroducing synthetic/random status/message sources |
| `/Users/andromeda/casanirvana/Guard/screens/messageScreen.js` | Thread view, send/receive, attachment upload, read-state updates | Attachment uploads now use `chat-attachments` owner-scoped paths (`{actor}/chat/{file}`), back-nav uses nested `bottomTab -> homeScreen`, incoming messages auto-mark read, and file-message parsing aligns with canonical DB fields. | `Wired` | Keep Expo Go-safe file reads (`expo-file-system/legacy`) and preserve read-state writes to both `read` and `is_read` |
| `/Users/andromeda/casanirvana/Guard/screens/callScreen.js` + `/Users/andromeda/casanirvana/Guard/hooks/useCallManager.js` | In-app call start path for residents/chat/visitor-host flows | Call initiation now enforces profile-based caller/callee resolution (`profiles.id`), supports unit-scoped host fallback for visitor flows, and keeps call-screen state driven by realtime call row updates instead of fake auto-connect timers. | `Wired` | Keep call records profile-scoped (`calls.caller_id/callee_id`), preserve deterministic status mapping (`initiated/ringing/answered/ended`), and avoid null-recipient inserts |
| `/Users/andromeda/casanirvana/Guard/screens/visitorDetailScreen.js` | Visitor call options from In/Out detail | Call Host remains in-app via `callScreen` (resident profile-scoped); Call Visitor is direct dial (`tel:`) using the submitted visitor phone for guest/cab/delivery/service flows. | `Wired` | Keep resident calls in-app for auditability; keep non-app visitor/personnel contacts as direct dial |
| `/Users/andromeda/casanirvana/Guard/hooks/useCalls.js` | Call create/update/subscription cache refresh | Call queries/mutations/subscriptions now resolve the authenticated guard to the canonical `profiles.id`, subscribe on both caller/callee edges, and invalidate message/conversation caches with the correct profile-scoped keys. | `Wired` | Keep all call history/cache reads profile-scoped (`calls.caller_id/callee_id` -> `profiles.id`) and preserve two-edge realtime subscriptions |

## 6) Profile + Settings
| Screen/Component | Data-Bound UI Elements | Current Wiring | Status | Wiring Checklist |
|---|---|---|---|---|
| `/Users/andromeda/casanirvana/Guard/screens/settingScreen.js` | Guard identity card, quick-contact call actions, app preference toggles (dark mode + biometric), logout | Replaced static profile/community placeholders with authenticated guard/community data; dark/biometric toggles now hydrate/save through `profiles.preferences`; quick-contact now uses direct dial (`Linking`) with validation; logout wired to auth-context signout + stack reset. | `Wired` | Keep settings toggles profile-backed and avoid route-level call placeholders |
| `/Users/andromeda/casanirvana/Guard/screens/languageScreen.js` | Language picker + update action | Language selection now persists to `profiles.preferences.language` via settings persistence service; hardware-back listener registration fixed to single mount. | `Wired` | Keep language preference profile-backed so locale persists across devices/sessions |
| `/Users/andromeda/casanirvana/Guard/screens/editProfileScreen.js` | Guard/user profile load + edit form + avatar upload + save | Replaced hardcoded values with DB hydration (`users` + `guards`), wired save mutations for editable profile fields, added storage upload path for avatar updates under `attachments/{auth.uid}/profile/*`, and added a password-reauthenticated login-email change request flow via `supabase.auth.updateUser`. | `Wired` | Keep login-email changes confirmation-based (do not patch `public.users.email` directly before verification); continue treating `guards.email` as auxiliary contact email only |
| `/Users/andromeda/casanirvana/Guard/screens/notificationSettingsScreen.js` | Notification category switches + sound/system toggles + bulk enable/disable | Removed UI-only placeholder behavior; settings now hydrate/save through `chat_settings.app_info_preferences.guardNotificationSettings` with optimistic rollback-safe updates. | `Wired` | Keep all notification preference writes centralized in `settingsPersistenceService` |
| `/Users/andromeda/casanirvana/Guard/screens/chatSettingsScreen.js` | Chat notification/privacy/media switches + reset action | Removed mock storage/history workflows; settings now hydrate/save through `chat_settings.app_info_preferences.guardChatSettings` and support reset-to-defaults with persisted write. | `Wired` | Keep chat settings DB-backed and avoid fake maintenance/storage side-effects |
