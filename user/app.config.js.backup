// app.config.js - environment configuration for Expo SDK 53+
import { config } from 'dotenv';

// Load environment variables from .env file
config();

export default {
  expo: {
    // Your existing expo config...
    extra: {
      // Any custom variables to expose to your app
      supabaseUrl: process.env.EXPO_PUBLIC_SUPABASE_URL,
      supabaseAnonKey: process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY,
      supabaseServiceKey: process.env.SUPABASE_SERVICE_KEY,
      sizeMattersBaseWidth: process.env.SIZE_MATTERS_BASE_WIDTH,
      sizeMattersBaseHeight: process.env.SIZE_MATTERS_BASE_HEIGHT,
    },
    "plugins": [
      "expo-router",
      [
        "expo-image-picker",
        {
          "photosPermission": "The app accesses your photos to let you share them with your friends."
        }
      ]
    ],
  },
};
