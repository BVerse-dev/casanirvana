// Mock for react-native-size-matters/extend
export const ms = jest.fn((size) => size);
export const s = jest.fn((size) => size);
export const vs = jest.fn((size) => size);
export const mvs = jest.fn((size) => size);
