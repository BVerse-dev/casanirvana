module.exports = function (api) {
  api.cache(true);
  
  return {
    presets: ['babel-preset-expo'],
    plugins: [],
    babelrcRoots: ['.'], // Prevent inheriting from workspace
    ignore: ['node_modules/**/*']
  };
};
